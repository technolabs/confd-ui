define([
    'tailf/core/logger'
], function(
    logger
) {

var _global = {
    addGenericTab : function(args) {
        logger.error('NO ConfD global iddGenericTab s defined!!!');
    },

    showKeypath : function(keypath, args) {
        logger.error('NO ConfD global showKeypath is defined!!!');
    }
};

return _global;

});
